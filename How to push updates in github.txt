==========================================
to get the latest update from github repo:
git pull

to clone the git repo:
git clone url

to check status of the repo:
git status

to add files to the repo:
git add .

to commit the changes:
git commit -m "message"

to push:
git push
==========================================
